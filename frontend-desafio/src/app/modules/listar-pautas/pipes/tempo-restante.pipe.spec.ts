import { TempoRestantePipe } from './tempo-restante.pipe';

describe('TempoRestantePipe', () => {
  it('create an instance', () => {
    const pipe = new TempoRestantePipe();
    expect(pipe).toBeTruthy();
  });
});
