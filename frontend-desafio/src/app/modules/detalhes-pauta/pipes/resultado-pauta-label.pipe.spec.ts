import { ResultadoPautaLabelPipe } from './resultado-pauta-label.pipe';

describe('ResultadoPautaLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ResultadoPautaLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
