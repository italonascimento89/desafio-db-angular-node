import { TotalVotosPipe } from './total-votos.pipe';

describe('TotalVotosPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalVotosPipe();
    expect(pipe).toBeTruthy();
  });
});
