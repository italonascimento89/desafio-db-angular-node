import { ValidationStateDirective } from "./validation-state.directive";

describe('ValidationStateDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationStateDirective();
    expect(directive).toBeTruthy();
  });
});
